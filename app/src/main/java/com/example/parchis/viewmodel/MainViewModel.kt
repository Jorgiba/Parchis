package com.example.parchis.viewmodel

import androidx.lifecycle.ViewModel

/**
 * Shared ViewModel para el flujo de menús y configuración.
 * Se mantiene ligero porque la lógica específica de cada flujo
 * reside en sus propios ViewModels (LoginViewModel, GameViewModel, etc.).
 */
class MainViewModel : ViewModel() {

}